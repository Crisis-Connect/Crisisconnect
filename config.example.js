// CrisisConnect — config template.
//
// To let users run the app WITHOUT entering their own API key:
//   1. Copy this file to `config.js`  (cp config.example.js config.js)
//   2. Paste your Groq key between the quotes below and save.
//   3. `config.js` is git-ignored, so your key never enters the repo.
//
// Get a free Groq key at https://console.groq.com/keys
//
// SECURITY NOTE: any key shipped to the browser is visible to anyone who opens
// the page (via dev tools / network tab). For a public demo, use a disposable
// free key and rotate it after judging. Never commit a real key to a public repo.

window.CRISISCONNECT_KEY = "gsk_your_key_here";
